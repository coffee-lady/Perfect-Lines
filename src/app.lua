local App = {
    config = require('src.scripts.config.config'),
    constants = require('src.scripts.constants.constants'),
    libs = require('src.libs.libs'),
    entities = require('src.scripts.entities.entities'),
}

return App
